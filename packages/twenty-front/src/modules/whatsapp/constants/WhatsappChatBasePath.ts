// Kept local instead of added to `AppPath` in twenty-shared: React Router
// accepts a raw string, so a native page needs no cross-package enum member.
export const WHATSAPP_CHAT_BASE_PATH = '/whatsapp';
