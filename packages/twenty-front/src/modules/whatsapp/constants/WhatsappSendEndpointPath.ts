// The `send-whatsapp-message` logic function is exposed through the legacy
// route-trigger prefix `/s`. Verified reachable on this deployment.
export const WHATSAPP_SEND_ENDPOINT_PATH = '/s/whatsapp/send';
